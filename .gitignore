# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/apps/*/node_modules
/packages/*/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# W23: 기사용 RN 앱 (Expo CNG — prebuild 결과물은 commit 안 함)
/apps/driver-rn/.expo/
/apps/driver-rn/android/
/apps/driver-rn/ios/
/apps/driver-rn/dist/
/apps/driver-rn/web-build/
/apps/driver-rn/GoogleService-Info.plist

# design 핸드오프 자료 (디자이너 zip — repo에 commit 안 함)
/design_handoff_shuttlee/
.env*.local

# Claude Code 개인 권한 override·런타임 cache (팀 공유 X)
.claude/settings.local.json
.claude/agent-memory/
.claude/worktrees/
