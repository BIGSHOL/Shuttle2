# Expo + pnpm monorepo: metro가 transitive 모듈(invariant·@babel/runtime·
# react-native-is-edge-to-edge 등)을 root node_modules에서 못 찾는 whack-a-mole
# 회피를 위해 npm/yarn 같은 flat hoist 구조 사용. RN/Expo 도구 호환성 가장 좋음.
# https://docs.expo.dev/guides/monorepos/#cant-resolve-package
node-linker=hoisted
